#include <cstdio>

int main(void)
{
    while(true)
    {
        // getchar() to get a string
        // if strlen == 1 and string[0] == '.' then exit
        // run func recursively to get result
    }
    return 0;
}